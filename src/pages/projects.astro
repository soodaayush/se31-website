---
import Layout from "../layouts/Layout.astro";
import Navbar from "../components/Navbar.astro";
import Footer from "../components/Footer.astro";
import ProjectCard from "../components/ProjectCard.astro";
import EmptyState from "../components/EmptyState.astro";
import PageIntro from "../components/PageIntro.astro";
import { PROJECTS } from "../data/projects";

const base = import.meta.env.BASE_URL;

---

<Layout title="Projects — SE31 Nest" description="A showcase of projects made by the SE31 cohort.">
  <Navbar />
  <main id="main-content" tabindex="-1">
    <PageIntro
      eyebrow="Made by the cohort"
      title="Cohort projects."
      description="A growing showcase of the things SE31 students are designing, coding and learning to ship."
    />

    <div class="mx-auto max-w-7xl px-4 py-12 sm:px-6 sm:py-16 lg:px-8">
      <div class="quiet-note mb-10 p-5">
        <p class="font-bold">Showcase your work</p>
        <p class="mt-1 leading-7">Have you built something cool? Add your project to the showcase. <a href={`${base}contribute`} class="brand-text-link font-bold text-brown dark:text-hardhat">Learn how to contribute.</a></p>
      </div>

      {PROJECTS.length > 0 ? (
        <div class="grid grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-3">
          {PROJECTS.map(project => (
            <ProjectCard project={project} />
          ))}
        </div>
      ) : (
        <EmptyState message="No projects added yet. Be the first to showcase your work!" />
      )}
    </div>
  </main>
  <Footer />
</Layout>
